# Deferred Components

This folder holds component code that is preserved for later review but should
not be considered active ESPHome component code.

## Current Deferred Components

- `hardware_info/` - Pico-related experimental component. It is internally
  inconsistent in the copied baseline and is deferred until the Pico workstream
  is revisited.

