# Changelog (2026/05/16)

## Added

- Added board starter templates under `sample_projects/board_templates/`.
- Added first-pass sensor packages across analogue, Bluetooth, I2C, I2S, and
  UART sensor folders.

## Changed

- Documented `sample_projects/board_templates/` as the home for reusable board
  starter files.
- Aligned board starter templates to include `wifi_dynamicip.yaml` when they
  include the shared WiFi package.
- Reconciled private workbench tracking after importing existing device files.

## Validation

- Generic YAML parsing passed for public sensor packages, board templates,
  private workbench device files, and the private SmartPad package.
- Active local include checks pass except for the known missing
  `boards/esp8266/sonoff_sv.yaml` package.

## Project Tracking

- Kanban board now has draft cards for each private workbench device.
- Kanban board has a dedicated follow-up card to rebuild the missing Sonoff SV
  board package.
